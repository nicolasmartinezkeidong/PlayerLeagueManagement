@model PlayerManagement.Models.PlayerPosition

@{
    ViewData["Title"] = "Details";
}

<h1>Player Position Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.PlayerPos)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.PlayerPos)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index" asp-controller="Lookups" asp-route-Tab="@Context.GetRouteValue("controller").ToString()-Tab">Back to Lookup List</a>
</div>
